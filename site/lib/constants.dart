import 'package:flutter/material.dart';

final colorTheme = ThemeData(
  colorScheme: ColorScheme(
    brightness: isDarkMode ? 
  )
);

bool showMenuButton = true;
String onPage = 'home';
const double defaultRoundedEdgeCircularRadius = 12;
